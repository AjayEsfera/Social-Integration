<% include partials/header.ejs %>
<div class="container">
  <h1><%=title %></h1>
  <img src="<%= pic %>">
  <h3><%= msg %> 
  	<% if(user)
  	{	%>
  		<%= user %>
  	<% } 
  	else if(googleuser)
  	{	%>
  		<%= googleuser %>
  	<% } %>


  	

<a href="../logout">Logout</a>
  